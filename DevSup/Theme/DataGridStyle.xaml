<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Column Header Style -->
    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource DataGridColumnHeaderBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource DataGridColumnHeaderForeground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource DataGridColumnHeaderBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=SortDirection}" Value="Ascending">
                <Setter Property="ContentTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=DataGridColumnHeader}, Path=Content}" Margin="0,0,5,0"/>
                                <!--  위 화살표 백터값으로 그림 -->
                                <Path VerticalAlignment="Center" Data="M 0 6 L 4 0 L 8 6 Z" Fill="{StaticResource DataGridColumnHeaderForeground}" Width="8" Height="8"/>
                            </StackPanel>
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=SortDirection}" Value="Descending">
                <Setter Property="ContentTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=DataGridColumnHeader}, Path=Content}" Margin="0,0,5,0"/>
                                <!-- 아래화살표 백터값으로 그림 -->
                                <Path VerticalAlignment="Center" Data="M 0 0 L 4 6 L 8 0 Z" Fill="{StaticResource DataGridColumnHeaderForeground}" Width="8" Height="8"/>
                            </StackPanel>
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
        </Style.Triggers>
    </Style>


    <!-- Brush Definitions -->
    <SolidColorBrush x:Key="GridBackgroundBrush" Color="{StaticResource GridBackgroundColor}" />
    <SolidColorBrush x:Key="GridForegroundBrush" Color="{StaticResource GridForegroundColor}" />
    <SolidColorBrush x:Key="GridBorderBrush" Color="{StaticResource GridBorderColor}" />
    <SolidColorBrush x:Key="SelectedRowBackgroundBrush" Color="{StaticResource SelectedRowBackgroundColor}" />

    <!-- DataGrid Style -->
    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource DataGridBackgroundColor}" />
        <Setter Property="BorderBrush" Value="{StaticResource DataGridBorderBrushColor}" />
    </Style>

    <!-- DataGridCell Style -->
    <Style TargetType="DataGridCell">
        <Setter Property="Background" Value="{StaticResource DataGridBackgroundColor}" />
        <Setter Property="Foreground" Value="{StaticResource GridForegroundBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource GridBorderBrush}" />
        <Setter Property="BorderThickness" Value="0,1,1,0" />
        <Setter Property="Padding" Value="2,0,0,2"/>
    </Style>

    <!-- DataGridRow Style -->
    <Style TargetType="DataGridRow">
        <Setter Property="Background" Value="{StaticResource GridBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource GridForegroundBrush}" />
        <Style.Triggers>
            <!-- 선택시 colers에서 선택한 값나옴 -->
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource SelectedRowBackgroundBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
