<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="{x:Type TextBox}" x:Key="ModernTextfreebox">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border CornerRadius="10"
                        Background="{DynamicResource TextBoxStyleBasicBackground}">
                        <Grid>
                            <Rectangle StrokeThickness="1" Fill="{DynamicResource TextBoxStyleBasicTextBackground}"/>
                            <ScrollViewer x:Name="PART_ContentHost"
                                      Margin="1"
                                      Background="{DynamicResource TextBoxStyleBasicTextBackground}"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Auto"
                                      Padding="5"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>